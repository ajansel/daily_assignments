require 'hand'

describe "Hand" do
  subject(:hand) { Hand.new(arr_of_cards) }

  describe "initialize" do
    it "creates a hand with five cards in it" do

    end

    it "sets the players stack to 100"
  end

  describe "#determine_hand" do
    it "contains logic to determine what hand they have"
  end

  describe "#compare_hands" do
    it "returns the better hand"
  end
end
